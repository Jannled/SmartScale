{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compress HTML into C-array",
            "type": "shell",
            "command": "gzip -kf data/index.html && xxd -i data/index.html.gz > include/index.html.h",
            "windows": {
                "command": "bash -c 'gzip -kf data/index.html && xxd -i data/index.html.gz > include/index.html.h'"
            },
            "problemMatcher": []
        }
    ]
}